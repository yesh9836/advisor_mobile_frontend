[pytest]
testpaths = tests
python_files = test_*.py
addopts = --maxfail=1 --strict-config --strict-markers -q
markers =
    unit: Fast unit-level tests
    integration: API/database integration tests
    mysql: MySQL-backed concurrency and locking tests
